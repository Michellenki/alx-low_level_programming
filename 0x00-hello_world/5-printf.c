#include <stdio.h>

/**
 * main this is the main function
 *
 * Return: 0 when successful
 */

int main(void)
{
printf("%s, but the outcome is a piece of art,\n", "with proper grammar");
return (0);
}
